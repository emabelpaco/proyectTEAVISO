import React from "react";
import { StyleSheet, Text, View } from "react-native";

export default function AddMensajeForm({toasRef, setLoading}) {
    return (
        <View>
            <Text>Add mensaje form</Text>
        </View>
    )
}

const styles = StyleSheet.create({})